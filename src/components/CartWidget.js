
import { Link  } from "react-router-dom"
const Icono = () => {
    return (
        <nav>
            <Link to="/carrito">
        <span className="material-icons">
        local_atm
        </span> 
        </Link>
        </nav>
    )
}

export default Icono